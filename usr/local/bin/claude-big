#!/usr/bin/env bash
set -e

docker run --rm -it \
  --network host \
  -e ANTHROPIC_BASE_URL=http://localhost:11434 \
  -e ANTHROPIC_AUTH_TOKEN=ollama \
  -e ANTHROPIC_API_KEY="" \
  -e ANTHROPIC_DEFAULT_HAIKU_MODEL=gpt-oss120b-64k-claude:latest \
  -e ANTHROPIC_DEFAULT_SONNET_MODEL=gpt-oss120b-64k-claude:latest \
  -e ANTHROPIC_DEFAULT_OPUS_MODEL=gpt-oss-120b-128k-claude:latest \
  -v "$PWD:/workspace" \
  -v claude-code-big.config:/root/.claude \
  claude-code "$@"
